<Window x:Class="SonicScout.SupportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SonicScout2.0 — Support &amp; Diagnostics"
        Width="760" Height="560" MinWidth="680" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="SUPPORT & DIAGNOSTICS" FontSize="22" FontWeight="Bold"
                       Foreground="{DynamicResource SurfaceTextBrush}"/>
            <TextBlock Text="Diagnostics stay local unless you explicitly choose Send Diagnostics to Developer."
                       Margin="0,6,0,14" TextWrapping="Wrap"
                       Foreground="{DynamicResource MutedBrush}"/>
        </StackPanel>

        <Border Grid.Row="1" Background="{DynamicResource PanelBrush}" CornerRadius="8" Padding="12" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock x:Name="SessionText" FontFamily="Consolas" FontSize="12"
                           Foreground="{DynamicResource CyanBrush}"/>
                <TextBlock Text="The support bundle excludes raw audio and profile audio content. Review it before sharing if desired."
                           Margin="0,5,0,0" TextWrapping="Wrap" Foreground="{DynamicResource MutedBrush}"/>
            </StackPanel>
        </Border>

        <TextBox Grid.Row="2" x:Name="DiagnosticsText"
                 IsReadOnly="True" AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11"
                 TextWrapping="NoWrap"
                 Background="{DynamicResource CardBrush}"
                 Foreground="{DynamicResource SurfaceTextBrush}"
                 BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1"
                 Padding="10"/>

        <WrapPanel Grid.Row="3" Margin="0,14,0,0">
            <Button Content="CREATE SUPPORT BUNDLE" Margin="0,0,8,8" Padding="12,7" Click="CreateBundle_Click"/>
            <Button Content="SEND DIAGNOSTICS TO DEVELOPER" Margin="0,0,8,8" Padding="12,7" Click="SendDiagnostics_Click"/>
            <Button Content="OPEN LOGS FOLDER" Margin="0,0,8,8" Padding="12,7" Click="OpenLogs_Click"/>
            <Button Content="REPORT ISSUE" Margin="0,0,8,8" Padding="12,7" Click="ReportIssue_Click"/>
            <Button Content="OPEN REPOSITORY" Margin="0,0,8,8" Padding="12,7" Click="OpenRepository_Click"/>
            <Button Content="CLOSE" Margin="0,0,8,8" Padding="12,7" Click="Close_Click"/>
        </WrapPanel>
    </Grid>
</Window>