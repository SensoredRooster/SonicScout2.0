<!-- LEQ Control Panel — Copyright (c) 2025-2026 ArtIsWar LLC -->
<!-- Licensed under GPL-3.0. See LICENSE file for details. -->
<Window x:Class="LEQControlPanel.Dialogs.StyledMessageBoxWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight"
        MinWidth="400"
        MaxWidth="550"
        WindowStartupLocation="CenterOwner"
        Topmost="True">

    <Window.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource PrimaryButtonStyle}"/>
    </Window.Resources>

    <Border Background="{StaticResource Brush_BackgroundDark}" BorderBrush="{StaticResource Brush_BorderDark}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Title bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Buttons -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource Brush_TitleBar}" CornerRadius="8,8,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Margin="15,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="IconText" Grid.Column="0" FontSize="18" Foreground="{StaticResource Brush_Accent}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="TitleText" Grid.Column="1" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource Brush_TextWhite}" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- Message Content -->
            <ScrollViewer Grid.Row="1" MaxHeight="400" VerticalScrollBarVisibility="Auto" Margin="20,15">
                <TextBlock x:Name="MessageText"
                           TextWrapping="Wrap"
                           Foreground="{StaticResource Brush_ForegroundMuted}"
                           FontSize="13"
                           LineHeight="20"/>
            </ScrollViewer>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,10,20,15">
                <Button x:Name="YesButton" Content="Yes" Click="YesButton_Click" Visibility="Collapsed" Margin="0,0,10,0" TabIndex="0"/>
                <Button x:Name="NoButton" Content="No" Click="NoButton_Click" Visibility="Collapsed" Margin="0,0,10,0" TabIndex="1"/>
                <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Visibility="Collapsed" Margin="0,0,10,0" TabIndex="2"/>
                <Button x:Name="OkButton" Content="OK" Click="OkButton_Click" Visibility="Collapsed" TabIndex="3"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>