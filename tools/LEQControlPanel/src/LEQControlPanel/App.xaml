<!-- LEQ Control Panel — Copyright (c) 2025-2026 ArtIsWar LLC -->
<!-- Licensed under GPL-3.0. See LICENSE file for details. -->
<Application x:Class="LEQControlPanel.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LEQControlPanel"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <SolidColorBrush x:Key="Brush_Surface" Color="#1F1F1F"/>
        <SolidColorBrush x:Key="Brush_Foreground" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="Brush_Accent" Color="#FFD700"/>
        <SolidColorBrush x:Key="Brush_AccentHover" Color="#FFC000"/>
        <SolidColorBrush x:Key="Brush_WarningAmber" Color="#FFA500"/>
        <SolidColorBrush x:Key="Brush_ForegroundDim" Color="#C0C0C0"/>
        <SolidColorBrush x:Key="Brush_Neutral" Color="#666666"/>
        <SolidColorBrush x:Key="Brush_Red" Color="#B22222"/>
        <SolidColorBrush x:Key="Brush_StatusRed" Color="#FF2A2A"/>
        <SolidColorBrush x:Key="Brush_StatusGreen" Color="#33CC33"/>
        <SolidColorBrush x:Key="Brush_TextWhite" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Brush_TitleGrey" Color="#888888"/>
        <SolidColorBrush x:Key="Brush_BackgroundDark" Color="#1A1A1A"/>
        <SolidColorBrush x:Key="Brush_BorderDark" Color="#333333"/>
        <SolidColorBrush x:Key="Brush_BorderMedium" Color="#555555"/>
        <SolidColorBrush x:Key="Brush_ForegroundMuted" Color="#CCCCCC"/>
        <SolidColorBrush x:Key="Brush_TitleBar" Color="#252525"/>
        <SolidColorBrush x:Key="Brush_PanelBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="Brush_Danger" Color="#D94444"/>

        <!-- Shared Dialog Styles -->

        <!-- Dialog button with opacity hover/press feedback -->
        <Style x:Key="DialogOpacityButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Green checkmark checkbox for dialog options -->
        <Style x:Key="DialogCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBorder"
                                    Width="16" Height="16"
                                    Background="#2A2A2A"
                                    BorderBrush="{StaticResource Brush_BorderMedium}"
                                    BorderThickness="1"
                                    CornerRadius="2"
                                    Margin="0,0,8,0">
                                <TextBlock x:Name="CheckMark"
                                           Text="&#x2713;"
                                           Foreground="#40C77F"
                                           FontSize="12"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="#40C77F"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource Brush_Neutral}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary button for No/Cancel actions -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Brush_BorderDark}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush_ForegroundMuted}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource Brush_BorderMedium}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#444444"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger button for destructive actions -->
        <Style x:Key="DangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Brush_Danger}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush_TextWhite}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary accent button for main actions -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Brush_Accent}"/>
            <Setter Property="Foreground" Value="{StaticResource Brush_BackgroundDark}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FFC000"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FFC000"/>
                                <Setter TargetName="Border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Hazard Hatch Pattern for Missing Module State -->
        <DrawingBrush x:Key="HazardHatchPattern" TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#1A1A1A">
                        <GeometryDrawing.Geometry>
                            <RectangleGeometry Rect="0,0,20,20"/>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing Brush="#2A2A2A">
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <LineGeometry StartPoint="0,0" EndPoint="20,20" />
                                <LineGeometry StartPoint="0,10" EndPoint="10,20" />
                                <LineGeometry StartPoint="10,0" EndPoint="20,10" />
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                        <GeometryDrawing.Pen>
                            <Pen Brush="#333333" Thickness="2"/>
                        </GeometryDrawing.Pen>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </Application.Resources>
</Application>
